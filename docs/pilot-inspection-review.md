# 巡检两周复盘（#58，2026-08-04 汇总）

> 现状：试点尚未真实启动（账号已发放、环境待激活）。本复盘覆盖巡检体系就绪情况 +
> 成本基线对账 + 本阶段故障记录，为试点首周（08-10 起）的运行基线。

## 1. 巡检体系就绪

| 项 | 状态 | 说明 |
|---|---|---|
| `pilot_daily_check.py` | ✅ 运行 healthy=true | 五项检查：backend_ready（DB/Redis）/ backup（26h 内有备份）/ uvicorn 错误日志（warn_only）/ 24h 成本 / 回流游标 |
| 计划任务 `AIBG-Pilot-DailyCheck` | ✅ 每日 08:30 | 失败告警走 ALERT_WEBHOOK_URL（**未配置，留位**） |
| 计划任务 `AIBG-Pilot-DailyBackup` | ✅ 每日 02:00 | 备份脚本实测出包（pilot-backup-20260804T010548Z） |
| 基线快照 | ✅ `data/pilot-baseline-20260804.json` | 与周报对账的起点 |

## 2. 成本对账（30 天窗口）

| 时间点 | 30 天成本 | 变化 |
|---|---|---|
| 08-04 早（基线） | ¥3.78 | — |
| 08-04 晚（复盘） | ¥4.17 | +¥0.39（当日验证/评测调用 qwen-plus 为主） |

- 24h 成本 ¥2.50（含本次 124 题真评 + 试点验证调用）。
- 单价锚点不变：咨询 ≈0.012 元/次（M-1 口径）；qwen-max 消耗来自对比实验（额度已耗尽）。
- **试点启动后**：以周报脚本按日聚合（pilot_weekly_report.py cost_by_day），每周对账毛利假设（团队版 999 元/上限 5000/2000/2000）。

## 3. 故障记录（本阶段）

| 日期 | 故障 | 处理 |
|---|---|---|
| 08-04 | 生产启动 chroma 旧数据崩溃（KeyError '_type'） | 备份旧目录重建空 data/chroma_db（此前已记录） |
| 08-04 | 生产库缺 subscription_plans 等 4 表（咨询/审查 POST 500） | 迁移 0056/0057 补齐（此前已记录） |
| 08-04 | CI 四 job 全红 | 8 处修复（分支/依赖/环境注入/email-validator/SessionLocal 绑定），已 4/4 绿 |
| 08-04 | uvicorn 日志历史错误误报 | 巡检日志检查降级为 warn_only（mtime 2h 内才计 fail） |

## 4. 试点首周巡检节奏（08-10 起）

- 每日 08:30 自动巡检（已配），人工每周一核对 JSON 摘要 + 周报口径。
- 回流游标随巡检报告（feedback_cursor）——审核动作 >0 即触发 #54 回流对比流程。
- 告警：若试点期间需要即时通知，配置 .env `ALERT_WEBHOOK_URL` 并加 `--alert` 参数（已内置）。
