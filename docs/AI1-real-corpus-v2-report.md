# AI-1 真实语料评测 v2 报告（#55，2026-08-04）

## 结论

- **qwen-plus 真评（124 题，含 5 条真实语料）：99.2%（123/124）**，报告：`eval/outputs/generation_eval_report_real_qwenplus.json`。
- 真实语料（real-co-0001 / real-cr-0001 / real-cr-0003 / real-dg-0001 / real-dg-0002）：**5/5 全过**。
- 分层：合同审查 F1 0.845、文书必填字段 100%、咨询分类准确率 100%、拒答层 8/8、法条失效层 4/4、AI-2 回流回归 12/12。

## v2 数据源扩充：未达成（如实）

- 目标 ≥30 条真实语料 → 实际仍 **5 条**（生产库无新增：试点未启动，咨询 6/审查 3/文书 2 与 v1 相同）。
- 扩充管线已就绪（`scripts/export_real_corpus_eval.py` 幂等重跑即扩），试点启动第 2-3 周数据积累后执行 v3 扩充。
- 评测集版本 v2.1 冻结不变；CI eval-regression 保持 --no-llm 确定性 124/124。

## 与 08-04 早间对比报告（72.3%）的差异说明

- 早间 72.3%（119 题，34 失败）来自 `compare_models.py` 批量并发对比；本次 99.2%（124 题，1 失败）为串行真评。
- 差异来源：① 题集 v2.0→v2.1（+5 真实语料）；② 早间批量并发时 LLM 限流/超时导致部分用例走了降级路径。
- **本次 124 题报告为最新权威基准**；下次对比统一用 `run_generation_eval.py`（串行）跑同题集。

## 发现的真实质量弱点（后续 prompt 调优输入）

- **cr_006 失败：缺失条款识别召回 0.0**——合同文本中"应有的条款未出现"时，qwen-plus 输出未标记 needs_facts。
- 对应合同审查 prompt（`prompt_service` 模板 legal_contract_review）需强化"逐条核对该条款是否在文本中，缺失须显式标注"指令；修复后在冻结集验证改善（对比 124 题基线）。

## 后续

- 试点首周后重跑 `export_real_corpus_eval.py --apply` → v3（目标 ≥30 条真实）→ 重新冻结 → CI 门禁换新题集。
- 模型切换（qwen-max 额度恢复）时以本次报告为基线对比。
