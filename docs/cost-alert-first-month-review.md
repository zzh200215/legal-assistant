# 成本预警阈值首月复盘（#71，2026-09-01）

## 机制

- 巡检 `pilot_daily_check.py --cost-warn`（默认 ¥50/日，`PILOT_COST_WARN_CNY` 可配）
- 超阈值 → `checks.cost_24h.ok=false` + findings 文案 + 告警通道（--alert 时）
- 统计口径：llm_observability 当日累计 qwen-plus/text-embedding-v3 估算成本（周报同口径）

## 首月运行结论（08-25 ~ 09-01）

| 项 | 值 | 判断 |
|---|---|---|
| 阈值触发次数 | 0 | 正常（试点未启动，日成本 0.00~0.06 元量级） |
| 机制验证 | 通过 | 08-25 预演：`--cost-warn 1` 正确触发 exit 1 + findings；默认 ¥50 不误报 |
| 实际最高日成本 | ¥0.0579（08-04，7 次 qwen-plus） | 距阈值 860 倍余量 |
| 告警链路 | 待真实试点验证 | 阈值→findings→--alert 通道端到端未真实触发过 |

## 结论与调整建议

1. **阈值参数合理**：¥50/日 ≈ 单日 4000+ 次咨询（按 ¥0.012/次），足以兜住试点规模（20 账号 × 5000 配额）异常流失；正式商用后按毛利模型重定。
2. **待真实触发演练**：试点启动后第 1 次真实触发时记录告警延迟与通道有效性，纳入巡检复盘。
3. **无代码变更需求**：机制、默认值、文档均无缺口。

## 关联

- #70 实现：scripts/pilot_daily_check.py（--cost-warn）
- #71 周报数据包：`scripts/pilot_weekly_report.py` 输出（试点未启动时周成本 ¥0.0 如实标注）
