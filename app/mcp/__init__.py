# MCP (Model Context Protocol) layer
# Transforms the existing agent tool system into an MCP-compatible architecture.
# Tools remain as BaseAgentTool instances; the registry adds standard MCP
# protocol semantics: tool discovery, schema-based permission enforcement,
# call logging, and structured error codes.
